{
	"name": "@neematajs-bun/http-server",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"check:type": "tsc --noEmit",
		"check:lint": "biome lint .",
		"check:fmt": "biome format .",
		"check": "bun run check:type && bun run check:lint && bun run check:fmt",
		"fmt": "biome check --apply ."
	},
	"devDependencies": {
		"@biomejs/biome": "1.7.1",
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
